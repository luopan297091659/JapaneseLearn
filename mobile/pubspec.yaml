name: japanese_learn
description: 言旅 Kotabi — Japanese Learning App
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3

  # Navigation
  go_router: ^16.1.0

  # Network
  dio: ^5.4.0
  retrofit: ^4.1.0

  # Local Storage
  flutter_secure_storage: ^10.0.0
  shared_preferences: ^2.2.2
  sqflite: ^2.3.2
  path_provider: ^2.1.2

  # Audio
  just_audio: ^0.10.5
  audio_service: ^0.18.12
  flutter_tts: ^4.0.2

  # UI
  flutter_svg: ^2.0.10+1
  cached_network_image: ^3.3.1
  lottie: ^3.0.0
  fl_chart: ^1.1.0
  shimmer: ^3.0.0
  flutter_card_swiper: ^7.2.0
  confetti: ^0.8.0

  # Japanese Text
  ruby_text: ^3.0.0+1

  # URL
  url_launcher: ^6.2.5
  android_intent_plus: ^5.0.2

  # Utils
  intl: any
  uuid: ^4.3.3
  logger: ^2.1.0
  connectivity_plus: ^7.0.0
  permission_handler: ^12.0.1
  file_picker: ^10.3.10
  archive: ^4.0.9
  path: ^1.9.0
  speech_to_text: ^7.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  riverpod_generator: ^2.3.9
  build_runner: ^2.4.8
  json_serializable: ^6.7.1
  retrofit_generator: ^9.6.0
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/images/app_icon.png"
  adaptive_icon_background: "#0F1946"
  adaptive_icon_foreground: "assets/images/app_icon.png"
  min_sdk_android: 24

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/audio/
    - assets/lottie/
